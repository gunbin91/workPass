{
  "name": "qr-checkin",
  "version": "1.0.0",
  "description": "QR 자동 출석체크 서버",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^1.4.5-lts.1",
    "playwright": "^1.60.0"
  }
}
